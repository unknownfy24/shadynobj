# shadynobj

Brochure site for the "Shady = No Blowjob" fantasy football league (shadynobj.com).

Static single-page site (`index.html`). Push to `main` auto-deploys to production via Vercel.

Content marked with `<!-- EDIT -->` comments is placeholder copy — swap in real team names, records, champions, and league settings.
